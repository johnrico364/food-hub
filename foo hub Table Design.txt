recipe_tbl
  id -> unique
  name -> varchar
  description -> longtext
  category -> array
  ingredients -> array
  country -> varchar
  prep_time -> int
  yt_link -> varchar
  image -> varchar

  created_at -> timestamps
  updated_at -> timestamps


api to make;
  /recipes/category/:type (:type = [all, main course, breakfast, soups, pasta, desserts, .etc])
  /recipes/country/:country-name
  /recipe-details/:recipe-id
  /search
